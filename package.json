{
  "name": "sigph-root",
  "private": true,
  "scripts": {
    "dev": "concurrently -c auto -n FRONT,BACK \"npm run dev --prefix frontend\" \"npm run dev --prefix backend\"",
    "dev:front": "npm run dev --prefix frontend",
    "dev:back": "npm run dev --prefix backend"
  },
  "devDependencies": {
    "concurrently": "^9.0.0"
  }
}
